<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <title>Links</title>
</head>

<body>
    <div class="container mt-5">
        <h1>Links</h1>
        <a class="btn btn-primary mb-2" href="/add">Adicionar link</a>
        <%for(let i=0; i < links.length; i++ ) {%>
            <div class="card p-3 mb-2" id="<%=links[i]._id%>">
                <form class="d-flex justify-content-between align-items-center"
                    action="/<%=links[i]._id%>?_method=DELETE" method="post">
                    <div class="flex align-items-center flex-column">
                        <h3>
                            <%=links[i].title%>
                        </h3>
                        <div>
                            <%=links[i].description%>
                        </div>
                        <a href="<%=links[i].title%>" target="_blank" class="text-muted">
                            <%=links[i].url%>
                        </a>
                        <div class="text-info">
                            <i class="material-icons">bar_chart</i>
                            Clicks: <%=links[i].click%>
                        </div>
                    </div>

                    <div class="d-flex align-items-center flex-column gap-2">
                        <a class="btn btn-info w-100 text-white" href="/edit/<%=links[i]._id%>">Edit</a>
                        <button class="btn btn-danger text-white" type="submit">Delete</button>
                    </div>
                </form>
            </div>
        <%}%>
    </div>
</body>

</html>